# 썸네일 제목 편집 — 로컬 구현·자체 검수 결과

검수일: 2026-09-07. 운영 배포, 커밋·푸시, 설치된 PC 워커 변경은 하지 않았다.

## 1. 기준본 보존

수정 전에 준비 서비스·포트폴리오 코드 111개와 실제 세 작업의 JSON 기록·완성 PNG를 비공개 로컬 폴더에 보존했다.
기준본: `C:\Users\becky\AppData\Local\WoolimWorker\preparation\title-edit-baseline-2026-09-07T11-30-06-471Z`.
원본 PPT, 기존 가림 승인, 슬롯, 기존 `mockup-state`, 기존 썸네일과 내지 파일은 덮어쓰지 않는다.

## 2. 제목 구조와 디자인

- 메인: 작업물 종류를 크게 표시. 54px부터 44px까지만 제한적으로 맞춘다.
- 보조: 브랜드·작업물명을 작은 주황색 22px로 표시. 표시 여부를 직접 선택한다.
- 기존 단일 제목을 자동으로 분해하거나 일괄 변경하지 않는다. 최초 새 제목 입력은 빈칸이다.
- 줄바꿈·제어문자·미지원 글리프·제목 영역 초과를 저장 전에 검증한다. 임의 잘림·말줄임·극단적인 축소는 하지 않는다.
- 대각선 장표와 최소 8px, 두 제목 줄 사이에도 최소 8px를 확보한다.
- 좌표·각도·로고·기존 제목 렌더러는 그대로 두고 별도의 제목 합성 모듈만 추가했다. 기존 BODY 렌더러의 코드 해시를 바꾸지 않았다.

자체 교차검수에서 악센트가 있는 높은 영문 글자의 메인·보조 겹침을 발견해 수정했다. 해당 재현 입력을 회귀 테스트에 포함했다.

## 3. 슬롯 편집 화면 연결

로컬 ‘목업 배정·미리보기’ 화면에 ‘썸네일 제목만 편집’을 추가했다.

1. 메인 제목·보조 제목·표시 여부 입력
2. 제목 저장
3. 제목 초안 미리보기
4. 제목 최종본 만들기
5. 최종본을 확대해 확인한 뒤 확인 체크
6. 이 제목으로 로컬 확정

입력만으로 저장·생성하지 않는다. 썸네일 제목과 기존 작업물명·게시글 제목은 별개다.
새 후보와 이전 완성본을 따로 보여 주며, 오류가 나도 입력 내용과 이전 완성본을 유지한다.

## 4. 실패·충돌·운영 보호

- 새 제목은 별도 `mockup-title-edit-state` 기록에 저장한다. 기존 `mockup-state`와 운영 활성 세트 참조는 변경하지 않는다.
- 후보 파일은 새 경로로 생성한다. 저장·생성이 끝나기 전에 이전 확정본을 바꾸지 않는다.
- 수정 버전, 바탕 이미지 지문, 최종 PNG 해시를 함께 검사한다. 오래된 화면의 저장·확정은 거절한다.
- 가림 승인이나 썸네일 슬롯이 바뀌면 오래된 후보·확정본은 더 이상 제공하지 않는다. BODY 슬롯만 바뀐 경우에는 제목 결과를 불필요하게 무효화하지 않는다.
- 같은 입력의 저장·생성·확정은 재사용한다. 자동·무한 재시도는 없다.
- 제목 편집 기록이 있는 작업은 기존 5장 운영 인계에서 `MOCKUP_TITLE_EDIT_LOCAL_ONLY`로 중단한다. 새 제목이 운영에 반영된 것으로 오인하거나 옛 제목을 잘못 인계하는 것을 막는다.
- 로컬 서버의 기존 세션·Host·Origin·CSRF 검사를 유지했다. 공개 응답에는 로컬 파일 경로나 가림 원문을 넣지 않는다.

## 5. 검증 결과

### 자동 검사

- 관련 통합·회귀 테스트: **261개 통과, 실패 0**.
- TypeScript 검사 통과.
- ESLint 오류 0. 기존 `components/ClientMarquee.tsx`의 `<img>` 권고 경고 1개는 이번 범위 밖이라 유지했다.
- 기존 본문·FAQ·제목·발행 상태·URL 보존 테스트 포함. 운영 DB를 읽고 전후 비교한 것이 아니라, 합성 데이터와 메모리 저장 대역으로 검증했다. 운영 데이터는 이번 작업에서 접근·변경하지 않았다.
- 새 제목 테스트에는 입력 중 요청 없음, no-op, 실패 시 이전 결과 유지, 이중 클릭 차단, 미리보기·확정 해시 결합, 미지원 문자, 슬롯·가림 변경, 손상 파일, 로컬 인계 차단을 포함했다.

### 실제 PPT에서 준비해 둔 세 규격

새 PPT 변환이나 블러 작업을 반복하지 않고, 앞서 검증·승인한 블러 PNG만 사용했다.

| 규격 | 메인 제목 | 보조 제목 | 기존 결과 보존 |
| --- | --- | --- | --- |
| 16:9 | 사업계획 발표자료 | 숨김 | 내지 4장·원본·슬롯·가림 기록 동일 |
| A4 가로 | 행사 대행 제안서 | 광양시민의 날 | 내지 4장·원본·슬롯·가림 기록 동일 |
| A4 세로 호환 | 건물관리 제안서 | 숨김 | 내지 4장·원본·슬롯·가림 기록 동일 |

세 작업의 기존 JSON·이미지 325개가 동일했고, 변경 가능한 빌드 기록에도 새 제목 관련 산출물만 추가되었다. 기존 모든 산출물 항목과 단계 데이터는 동일했다.
기존 썸네일과 새 썸네일을 픽셀 비교했을 때 세 규격 모두 **제목 영역 밖 변경 픽셀 0**이었다. 내지 12장 파일 해시도 동일했다.
1080px 원본과 360px 축소본을 각각 육안 확인했다. 제목 잔상·한글 깨짐·로고 또는 장표와의 충돌은 발견하지 못했다.
로컬 최종 확인 후에도 기존 기록·완성본 보존 검사를 다시 통과했다. 이는 발행 승인이 아니다.
실제 샘플 검수 프로세스에서 외부 요청과 자식 프로세스 실행을 예외로 차단했으며 해당 호출 횟수는 0이었다.

### 브라우저 자체 검수

고객 원본 작업이 아닌 별도 합성 작업으로 실제 브라우저에서 입력 → 저장 → 초안 → 최종본 → 확대 확인 → 로컬 확정을 확인했다.
입력 중 미저장 안내, 기존 완성본 유지, 최종 이미지를 열기 전 확정 불가, 저장 실패 시 입력·이전 결과 유지도 확인했다.

## 6. 결과물과 남은 경계

세 규격 비교 이미지: `C:\Users\becky\AppData\Local\WoolimWorker\preparation\thumbnail-title-edit-results\three-formats.png`.
각 규격 원본 PNG: `qa5-real-1`, `qa5-real-2`, `qa5-real-3` 아래 `thumbnail-title-edit-v1\thumbnail.png`.
검수 근거 JSON은 저장소의 무시된 `.codex-tmp/mockup-stage-5/title-edit-*-audit.json`에 있다. 원본이나 고객 자료는 공개 디렉터리에 넣지 않았다.

이번 범위에서 발견된 미해결 오류는 없다. 다만 모든 환경에서 오류가 절대 없다는 보장은 아니다.
운영 편집 화면·운영 썸네일 단독 교체·PC 워커 배포는 이번 범위에 포함하지 않았다. 후속 운영 연결은 별도 승인과 인계 설계가 필요하다.
환경변수 파일을 읽거나 출력·커밋하지 않았고, Gemini 및 유료 이미지 API를 호출하지 않았다.
